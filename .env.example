# =============================================================================
# Form Discovery Configuration
# =============================================================================

# Required: Anthropic API Key
ANTHROPIC_API_KEY=your-api-key-here

# Required: URL of the form to discover
FORM_URL=https://demoqa.com/automation-practice-form

# =============================================================================
# Optional Configuration
# =============================================================================

# Name for generated files (default: derived from URL)
# FORM_NAME=student-form

# Seed to run before discovery (default: 'default' which just navigates to URL)
# Create custom seeds in src/seeds/{name}.ts for login/navigation
# SEED_NAME=default

# Output directory for generated tests (default: tests/generated)
# OUTPUT_DIR=tests/generated

# Anthropic model to use (default: claude-sonnet-4-20250514)
# ANTHROPIC_MODEL=claude-sonnet-4-20250514

# Max discovery iterations (default: 20)
# MAX_ITERATIONS=20

# Browser to use: chromium, firefox, webkit (default: chromium)
# BROWSER=chromium

# Run browser in headless mode (default: false)
# HEADLESS=false

# =============================================================================
# Custom Seed Credentials (example for login seeds)
# =============================================================================

# APP_USERNAME=testuser
# APP_PASSWORD=testpass
